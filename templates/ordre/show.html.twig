{% extends 'base.html.twig' %}

{% block title %}Ordre{% endblock %}

{% block content %}
    <h1>Ordre des {{ ordre.nom }} {% if (ordre.vernaculaire is defined and ordre.vernaculaire != "") %} ({{ ordre.vernaculaire }})   {% endif %}
    <br/>
    {% if ordre.sousClasse is defined %}
    Sous-classe des {{ordre.sousClasse.nom}},  Classe des {{ordre.sousClasse.classe.nom}}
    {% else %}
    {{ordre.classe}}
    {% endif %}
    </h1>

    
               
            
         

    <a href="{{ path('ordre_index') }}">back to list</a>

    <a href="{{ path('ordre_edit', {'id': ordre.id}) }}">edit</a>

    {{ include('ordre/_delete_form.html.twig') }}
{% endblock %}
