{% extends 'base.html.twig' %}

{% block title %}SousClasse{% endblock %}

{% block body %}
    <h1>SousClasse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_classe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sous_classe.nom }}</td>
            </tr>
            <tr>
                <th>Vernaculaire</th>
                <td>{{ sous_classe.vernaculaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sous_classe_index') }}">back to list</a>

    <a href="{{ path('sous_classe_edit', {'id': sous_classe.id}) }}">edit</a>

    {{ include('sous_classe/_delete_form.html.twig') }}
{% endblock %}
