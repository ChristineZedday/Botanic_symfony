<form method="post" action="{{ path('sous_classe_delete', {'id': sous_classe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_classe.id) }}">
    <button class="btn">Delete</button>
</form>
