<form method="post" action="{{ path('sous_embranchement_delete', {'id': sous_embranchement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_embranchement.id) }}">
    <button class="btn">Delete</button>
</form>
