<form method="post" action="{{ path('embranchement_delete', {'id': embranchement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ embranchement.id) }}">
    <br/>
    <button class="btn">Delete</button>
</form>
